
2016-10-09

v_dev
- del-key-event fix.
- SetCssBackgroundClass added to shape.

v1.0.3.0
- adding unique id to the svg that it can easly get accessed by id and to be sure that we operate on the correct one ;)
- Delete event only contained the shape if shape was deleted. now it also has the dependend connections because the get deleted, too.
v1.0.2.0
- removed all foreignobjects, because they are not well supported in some browsers (chrome, ie,...).
- workedaround the foreignobject-issues by absolute positioning divs and put them behind the svg-items (z-indexing)
- Tested in current version of Edge, IE, Firefox, Chrome